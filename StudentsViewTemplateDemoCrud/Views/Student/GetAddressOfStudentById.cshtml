@model StudentsViewTemplateDemoCrud.Models.Address

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>GetAddressOfStudentById</title>
</head>
<body>
    <div>
        <h4>Address</h4>
        <hr />
        @*<dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Country)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Country)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.State)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.State)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.City)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.City)
            </dd>

        </dl>*@

        <dl class="dl-horizontal">
            <dt>Country</dt>
            <dd>@(Model?.Country ?? "Address not available")</dd>

            <dt>State</dt>
            <dd>@(Model?.State ?? "Address not available")</dd>

            <dt>City</dt>
            <dd>@(Model?.City ?? "Address not available")</dd>
        </dl>
    </div>
    <p>

       
        @Html.ActionLink("Edit Address", "EditAddress", new { id = Model?.Id ?? 0 }) |
        @Html.ActionLink("Delete Address", "DeleteAddress", new { addid = Model?.Id ?? 0 }) |
        @Html.ActionLink("Back to List", "GetAllStudents")
    </p>
</body>
</html>
